use std::fs;
use std::env;


fn main() {

    let mut i:i64 = 0;
    let n:i64 = 10000000000;
    let args: Vec<String> = env::args().skip(1).collect();
    let firstarg = &args[0];
    let mut firstPass = false;
    for _ in 0..n{
        i +=1;
    
    }
    
}

